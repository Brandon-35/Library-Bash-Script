#!/bin/bash

LOG_FILE="$HOME/git_commit_log.txt"
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/1341087748966191238/3yxajS6t2t8_lBHsvlwX0d0d8EFqp5p5eQ6fXox5amPDn5AHCg2aa8mkDHRafAbHfsuz"

# Lấy thông tin push
TIMESTAMP=$(date "+%Y-%m-%d %H:%M:%S")
BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD)
REMOTE_URL=$(git remote get-url origin)

# Ghi log push
echo "🚀 [$TIMESTAMP] Pushed branch '$BRANCH_NAME' to remote: $REMOTE_URL" >> "$LOG_FILE"
echo "-----------------------------------" >> "$LOG_FILE"

# Kiểm tra nếu webhook bị lỗi
if [ -z "$DISCORD_WEBHOOK_URL" ]; then
    echo "⚠️ Discord webhook chưa được cấu hình!"
    exit 1
fi

# Tạo payload JSON
PAYLOAD=$(jq -n --arg timestamp "$TIMESTAMP" \
                    --arg branch "$BRANCH_NAME" \
                    --arg remote "$REMOTE_URL" \
                    '{content: ("🚀 New push! \n📅 Time: " + $timestamp + "\n🌿 Branch: " + $branch + "\n🔗 Remote: " + $remote)}')

# Gửi thông báo đến Discord
curl -H "Content-Type: application/json" -d "$PAYLOAD" "$DISCORD_WEBHOOK_URL"
